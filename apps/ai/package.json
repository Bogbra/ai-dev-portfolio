{
  "name": "@ai/python-ai",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn main:app --reload --port 4000",
    "build": "echo 'Python — no build step required'",
    "start": "uv run uvicorn main:app --host 0.0.0.0 --port 4000",
    "lint": "echo 'Python lint — run: uv run ruff check .'"
  }
}
