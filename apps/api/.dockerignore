# apps/api — used when building with apps/api as the build context directly.
# (In practice the monorepo root .dockerignore applies because the context is .)

node_modules/
dist/
*.tsbuildinfo
.env
.env.*
!.env.example
coverage/
.DS_Store
